---
title: "Fix typo in `healthcheck` API response (#1377)"
categories: [backend, 25.05, release]
sidebar: release-notes
toc-expand: true
date: "April 28, 2025"
# CHECK: Title validation failed - Both versions of the content convey the same basic message, but the edited content is slightly more polished and clear. Here are some points of comparison:

1. **Capitalization**: The edited content starts with a capital letter, which is more grammatically correct for a standalone sentence or title.

2. **Clarity**: The edited version specifies "API response" instead of just "endpoint response," which may be clearer to some readers who are familiar with APIs.

3. **Formatting**: The
# Content overwritten from an earlier version - 2025-06-06 08:50
# PR URL: https://github.com/validmind/backend/pull/1377
---

# PR Summary

This pull request fixes a typo in the health check API endpoint response. The endpoint previously returned a JSON object with a misspelled key ('stauts') and now correctly returns the key as 'status'. This change ensures that consumers of the API receive the expected response format for health checks.